#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	int ch = 'a';

	 while (ch <= 'z' && ch != 'q' && ch != 'e')
	 {
		puthcar(ch);
		ch++
		continue
	 }

	putchar('\n');

	return (0);
}
