#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char toPut[] = "\"Programming is like building a multilingual puzzle";

	puts(toPut);

	return (0);
}
